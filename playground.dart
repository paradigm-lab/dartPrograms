
void main() {

    // Defining the variable using the var, This will tell the compile that this is the string type this is type inference
    // Dart support both type inference and static type defined
    var firstName = "Collins";

    // We define the variable as the string
    String lastName = "Boniface";

    print(firstName + " " + lastName);
}
